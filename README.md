# 🏆 פרויקט לוחמים — Alpha Protocol App

## קבצים להעלאה ל-GitHub (ללא config.js!)

```
lohamim-app/
├── index.html      ← העלה
├── sw.js           ← העלה  
├── manifest.json   ← העלה
├── README.md       ← העלה
├── config.js       ← ⚠️ לא להעלות! (gitignored)
└── .gitignore      ← העלה
```

## משתמשים
- **Afek** — admin (רואה הכל)
- **Lavi** — viewer (רואה שם ארוחה + ✅/❌ בלבד)

## הערה חשובה
`config.js` נמצא רק על המחשב שלך. לעולם לא להעלות אותו ל-GitHub.
